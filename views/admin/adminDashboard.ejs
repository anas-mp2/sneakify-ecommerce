<!-- newpage.ejs -->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sneakify New Page</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">
    <link rel="stylesheet" href="/styles/adminStyles/dashboard.css">
    <style>
        body {
            background-color: #1e1e1e;
            color: #ffffff;
            margin: 0; /* Remove default body margin */
        }
        /* Ensure flex-grow-1 takes full width with no padding/margin on the right */
        .flex-grow-1 {
            padding: 0;
            margin: 0;
        }
        /* Adjust container padding to add space on both left and right */
        .container-fluid.custom-container {
            padding: 1.5rem 1.5rem 1.5rem 1.5rem; /* 1.5rem padding on both left and right */
            margin-right: 0;
        }
        /* Ensure the d-flex container takes full width */
        .d-flex {
            width: 100%;
            margin: 0;
        }
        /* Placeholder for the table area */
        .table-placeholder {
            width: 100%;
            min-height: 300px; /* Give it some height for visibility */
            background-color: #2c2c2c; /* Match the table background color */
            border-radius: 5px;
        }
    </style>
</head>
<body>
    <div class="d-flex">
        <!-- Sidebar -->
        <%- include('../partials/admin/sidebar') %>

        <!-- Main Content -->
        <div class="flex-grow-1">
            <%- include('../partials/admin/navbar') %>
            
            <div class="container-fluid custom-container">
                <div class="input-group mb-3">
                    <input type="text" class="form-control bg-dark text-white" placeholder="Search for User Name">
                </div>
                
                <!-- Empty Table Area (Placeholder) -->
                <div class="table-placeholder">
                    <!-- Add your content here later -->
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>